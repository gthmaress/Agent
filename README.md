# DeepAgent 

> Проект по написанию ИИ-агента на основе DeepAgent.

## Installation

1. Clone repository

git clone <repository>
cd DeepAgent

2. Install Poetry

3. Install dependencies

poetry install

4. Configure environment variables

Copy .env.example to .env
and add your API keys.

5. Run

poetry run python main/run.py